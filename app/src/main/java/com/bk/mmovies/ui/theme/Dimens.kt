package com.bk.mmovies.ui.theme

import androidx.compose.ui.unit.dp

// Shared by PrimaryButton, ErrorComponent, and AuthWelcomeScreen so their
// buttons stay visually identical — not a general design-token system.
val primaryButtonHeight = 64.dp
val primaryButtonCornerSize = 14.dp
val primaryButtonIconPadding = 12.dp
val primaryButtonIconSize = 18.dp